# Parity Re-Audit — 2026-05-28

**Independent second-pass gap analysis: Python Shiny (`reference/tfbpshiny`) ↔ Go + React rewrite (HEAD = `e10cfa4`).**

This is a **fresh, independent re-examination** of the gap between the running Shiny app and the rewrite. It deliberately does **not** trust the two prior sets of documents, which disagree:

- `docs/parity/*.md` (2026-05-21) declared the rewrite "structurally complete but functionally early" — most analytical modules broken/stubbed.
- `docs/parity/auto-status/*.md` (2026-05-22, Phases A/B/C) claim those audits are now closed end-to-end.

Both were generated by agents in one pass; this re-audit re-read the **actual source on disk** on both sides for every claim.

## ✅ Implementation status (closed 2026-05-28, branch `fix/real-data-bug-classes`)

The fixes below were implemented TDD-style and verified: `go test ./... -race`, `data_prep` pytest, `tsc`/`vitest`/`vite build`, snapshot `make parity` (15/15), a real-artifact rebuild (`local-20260528-v5`, schema_version=5), and live spot-checks (Select matrix loads on first visit; common-regulators narrows the matrix; binding/perturbation hovers show `SYMBOL (LOCUS_TAG)`).

**Closed (both P0s + the P1/P2 action list):**
- ✅ **P0-3 / DM-0** — `write_field_manifest` now sources fields from `{db}_meta` only (declared `sample_id_field` as the join key; experimental-condition columns kept even when hidden). Build-time assertion guards `default_filters ⊆ field_manifest`. Verified live: the matrix returns 200 on first load.
- ✅ **P0-2** — `regulator_locus_tag` accepted as a hidden-but-valid WHERE field in matrix/breakdown/export/comparison via `checkFilterFields`; verified live (matrix narrows to the chosen regulators).
- ✅ **SQL-1** — `filter_hackett_to_analysis_set` restricts the materialized `hackett`/`hackett_meta` to the analysis set at build time.
- ✅ **B-1 / P-1 / SQL-3** — the four scatter templates no longer filter NULL/Inf/NaN; `SafeFloat` carries non-finite/NULL through as JSON `null`; `pearsonR` is pandas-faithful (drop NaN pairs, inf→null).
- ✅ **B-2 / P-4** — corr response carries `regulatorDisplay` (SYMBOL (LOCUS_TAG)); picker + hovers + symbol-sort wired.
- ✅ **B-3 / P-2 / P-3** — default-regulator auto-select + stale reconcile.
- ✅ **AC-1 / AC-4** — nav + Home links preserve `location.search`.
- ✅ **SD-3** — `field_manifest` is `{db}_meta`-only, so data-only columns are no longer offered as filters (verified: zero uncached visible filter fields on the real artifact).
- ✅ **SD-4** — **subsumed by SD-3**: with data-only columns gone, no visible filter field is high-cardinality/uncached on the real artifact, so the proposed live-distinct endpoint is unnecessary (would be dead code).
- ✅ **SD-1 / SD-2** — `/regulators/resolve` is filter-aware (per-arm WHERE, regulator stripped) and uncapped (1000-cap removed).
- ◑ **SD-6** — Reset now CLEARS filters (was "revert"). *Deferred:* apply-to-all persistence (FilterSpec wire-type change + prev_apply_to_all) and the cascade (SD-6A).
- ✅ **SQL-2** — execution-level TopN parity test (callingcards×hackett) pins n / n_responsive / responsive_ratio.
- ✅ **C-1** — harbison dedup branch drops user binding filters (matches Shiny). ✅ **C-2** skip+warn unconfigured datasets. ✅ **C-3 / SQL-4** numeric filters use `TRY_CAST(… AS DOUBLE)`. ✅ **C-5 / C-6** lenient top_n / threshold parsing. ✅ **C-8** Comparison heading.
- ✅ **DM-1** condition_cols excludes hidden fields (hackett → `time`). ✅ **DM-2** dataset descriptions carried. ✅ **DM-4** field descriptions/level-definitions populated (best-effort from labretriever). ✅ **DM-5** single source of truth (`EXPERIMENTAL_CONDITION_FIELDS`). ✅ **DM-6** SCHEMA.md at v5. ✅ **H3** nav label "Dataset selection".

**Deferred (documented, not closed) — heaviest UX + low-value items:**
- **SD-5** — in-modal Regulator selectize card (substantial new UI wired into Select's filter-apply path; the pairwise common-regulators flow already provides regulator filtering).
- **SD-6c / SD-6a** — apply-to-all persistence + cascade. The cascade is inert on real data: the rewrite's filterable fields are lowercase machine columns; Shiny's upstream categoricals are Title-Case display columns (excluded by `SafeIdentRE`), so `upstream_cols` is empty. (DM-3 not added.)
- **SQL-5** — `regulator_display_names` keeps deterministic `MIN(symbol)`; Shiny's `FIRST` is non-deterministic, so exact parity is impossible without a byte-unstable artifact. Only matters for a locus tag mapping to multiple symbols.
- Remaining pure-cosmetic chrome (version pill H1, layout H7, etc.) — none block cutover.

> **Schema bump:** these fixes raised the artifact to **schema_version=5** (`dataset_manifest.upstream_cols`/`description`; populated `field_manifest.description`/`level_definitions`). The Go binary's compatible range is now `[5,5]`. The real artifact must be rebuilt (done: `local-20260528-v5`).


## Reconciliation log

> **This audit overlapped a live bug-fixing session, now complete.** While the workflow ran (~27 min), commit `9cbc3ae` ("close the four e10cfa4 bug CLASSES") landed on `main` — that one commit contains the entire bug-fix session. Agents that read a file before the fix saw the bug; the orchestrator's post-workflow re-read saw the fix — which is exactly how the **NaN→500 P0 false positive** arose. **This audit is final as of `9cbc3ae` (HEAD).** Every still-open finding below was re-verified by direct code read against that exact commit *after* the session closed; none was affected by it (the session's changes are listed under "Closed by `9cbc3ae`").

**Closed by `9cbc3ae` (verified):**
- ✅ **NaN/Inf → 500** (the workflow's #1 P0, now withdrawn). `9cbc3ae` not only added the corr-drop at `binding_corr.go:243` but generalized the fix: a new `domain.SafeFloat` marshals non-finite values as JSON `null` on `BindingRow.Value`, `PerturbationRow.Value`, `DTORow.{DTOEmpiricalPValue,DTOFDR}`, and `TopNRow.ResponsiveRatio` — closing the *whole class* (data tabs + DTO + TopN), which is broader than this audit even flagged.
- ✅ **DM-7** (fixture `callingcards.sample_id_field='gm_id'` vs production `sample_id`). The fixture `callingcards_meta` now exposes `sample_id`, removing the inversion.
- ✅ **Reserved-keyword identifier robustness** (not a finding here, but real-data relevant): new `quotedIdent` double-quotes every SQL identifier interpolation, so a column like the genomic `end` parses.
- ◑ **Fixture widening** (this audit's top recommendation, partially done): `harbison` is now a populated **second binding dataset** (`default_active=True`) with an IEEE-NaN cell, a zero-variance column, and an `end` keyword column. Binding multi-pair flows are now testable; **perturbation is still single-dataset**, so perturbation multi-pair remains unexercised.

**Confirmed STILL OPEN at `9cbc3ae` (unaffected by the commit — verified by diff):**
- ❗ **P0-3 (reproduced live on real data)** — default filters 400 the matrix on first load. `write_field_manifest`'s `join_keys = data_cols ∩ meta_cols` heuristic (`manifests.py:355`) drops `condition`/`time`/`treatment` from `field_manifest` on the real artifact, but `default_filters` seed those fields. `9cbc3ae` didn't touch this logic. See [CRITICAL.md → P0-3](CRITICAL.md).
- ❗ **P0-2** — common-regulators filter 400 (matrix/breakdown/export). `9cbc3ae` only re-quoted identifiers in those handlers; no `stripRegulatorFilter` was added. See [CRITICAL.md](CRITICAL.md).
- All P1s in the action list below (scatter NULL/inf filter, regulator symbols, default-regulator, nav state-loss, data-table-only fields, filter-blind/1000-cap resolve, condition_cols hidden fields, hackett view filter, TopN test gap, harbison-dedup C-1).

## How this was produced

A multi-agent workflow audited **8 surfaces** (5 modules + 3 cross-cutting concerns). Every finding was then **adversarially re-verified** by an independent agent instructed to *refute* it against the current code, and each surface got a **completeness-critic** sweep ("what user-visible Shiny behavior did the audit miss?"). The orchestrator also hand-verified the two highest-stakes areas directly (correlation SQL, comparison hackett-filter).

- 62 agents · 46 primary findings · 39 additional behaviors surfaced by critics.
- Primary-finding verdicts: **31 confirmed · 11 partial (real but mis-stated) · 4 refuted (not a gap).**
- **The orchestrator then re-verified every P0/P1 directly against HEAD.** This caught a **false positive on the workflow's #1 headline P0** (NaN→500): the guard the agents claimed was missing exists at `binding_corr.go:243`. That finding is **withdrawn** — see [CRITICAL.md](CRITICAL.md) and [non-gaps.md](non-gaps.md).
- Severity after verification + orchestrator correction (confirmed + partial + critic-surfaced):

  | Severity | Count | Meaning |
  |---|---|---|
  | **P0** | **2 (verified; 1 reproduced live on real data)** | Breaks the Select-Datasets matrix — one on first load, one on the common-regulators flow |
  | **P1** | ~15 | Important UX/behavior/numeric divergence |
  | **P2** | ~26 | Minor behavioral / edge-case divergence |
  | cosmetic | ~30 | Visual only |

> **Real-data update (2026-05-28):** after the real `tfbp.duckdb` was built and the stack booted, **P0-3 reproduced live** — the dataset matrix 400s on first load (default selection) because the seeded `default_filters` reference experimental-condition fields the real-data `field_manifest` drops. This was invisible to the fixture. See [CRITICAL.md → P0-3](CRITICAL.md).

## Headline assessment

**The 2026-05-21 audit is stale; the rewrite is now a genuinely faithful port of the core science.** The big-ticket P0 claims from the old audit are closed: the correlation SQL (Pearson + Spearman) is a near-line-for-line port of Shiny's `_corr_pair_sql_impl`; the comparison plot is a real faceted boxplot (not the old heatmap) and the hackett perturbation-filter-skip matches `queries.py:432`; the schema_version=4 manifest plumbing is complete; Home/Binding/Perturbation/Comparison/Select all exist and structurally match.

**But the rewrite is not at parity.** Two genuine P0 blockers (one reproduced live on real data) and a cluster of P1 divergences remain — and notably, the **most consequential ones were missed by both prior passes** because they only manifest against the *real* artifact (where `{db}`/`{db}_meta` share metadata columns, where regulator sets exceed 1000, where zero-variance groups exist) or on interaction paths no test exercises. The synthetic fixture is the through-line: it masked the first-load matrix 400 (P0-3) entirely, since its `{db}`/`{db}_meta` tables don't overlap the way labretriever's materialized tables do.

### The two verified P0 blockers (see [CRITICAL.md](CRITICAL.md))

Both crash the Select-Datasets intersection matrix with the same HTTP 400 (`CheckField` rejecting a filter field), but have different triggers and fix sites:

1. **P0-3 — default filters 400 the matrix on first load (reproduced live on real data).** `field_manifest` drops the experimental-condition columns (`condition`/`time`/`treatment`) because `write_field_manifest` treats every column shared by `{db}` and `{db}_meta` as a join key and excludes it (`manifests.py:355`) — but the seeded `default_filters` reference exactly those fields, so the matrix 400s on the default selection with zero user action. Fixture-masked; only surfaced once the real artifact was built.
2. **P0-2 — common-regulators flow 400s end-to-end.** Clicking an off-diagonal cell → "Select N common regulators" writes a `regulator_locus_tag` filter to every active dataset, but that field is hidden from `field_manifest` and the matrix/breakdown/export handlers reject unknown filter fields (only `/corr` strips it). *Orchestrator-verified against HEAD.*

> **Withdrawn:** the workflow's other claimed P0 — *NaN correlation → 500 for the whole correlations page* — was **refuted on direct inspection**. The guard the agents reported missing is present and correct at `binding_corr.go:243` (`if math.IsNaN(...) || math.IsInf(...) { continue }`), explicitly mirroring Shiny's `df.dropna`. Do not chase it. Details in [CRITICAL.md](CRITICAL.md).

## Prioritized action list

| # | Severity | Surface | Gap | Doc |
|---|---|---|---|---|
| 1 | **P0 (live)** | select datasets / data prep | default filters reference fields `field_manifest` drops (`condition`/`time`/`treatment`) → matrix 400s on first load | [CRITICAL](CRITICAL.md) |
| 2 | **P0** | select datasets | `regulator_locus_tag` filter 400s matrix/breakdown/export → common-regulators flow crashes end-to-end | [CRITICAL](CRITICAL.md) |
| 3 | P1 | data prep | hackett views never filtered to analysis-set rows (perturbation tab + select metadata diverge) | [sql-numerical-parity](sql-numerical-parity.md) |
| 4 | P1 | binding + perturbation | per-pair **scatter** SQL filters NULL/inf/nan that Shiny keeps → Spearman ranks/r + plotted points diverge | [binding](binding.md) |
| 5 | P1 | binding + perturbation | regulator **gene symbols** never wired to picker/hover (bare locus tags shown; no API path supplies the map) | [binding](binding.md) |
| 6 | P1 | binding + perturbation | no default-regulator auto-select → scatter grid + overlay blank on first load; stale regulator never reconciled | [perturbation](perturbation.md) |
| 7 | P1 | app chrome | selection + filters **lost on nav** (no shared store; Nav/Home links drop `?binding/?perturbation/?filters`) | [app-chrome](app-chrome.md) |
| 8 | P1 | select datasets | filter modal exposes **data-table-only columns** (`target_locus_tag`, `score`, …) → filtering them 500s; high-cardinality categoricals unfilterable | [select-datasets](select-datasets.md) |
| 9 | P1 | select datasets | common-regulators resolve is **filter-blind** (matrix cell is filter-aware) and silently **capped at 1000** tags | [select-datasets](select-datasets.md) |
| 10 | P1 | select datasets | no in-modal regulator picker; cascade narrowing missing; Reset semantics inverted; apply-to-all not persisted (stale cross-dataset filter) | [select-datasets](select-datasets.md) |
| 11 | P1 | data prep | `condition_cols` includes HIDDEN fields → wrong hover labels (`ZEV / P / 45` vs Shiny's `45`); dataset descriptions + field descriptions/level-defs dropped | [data-prep-manifest](data-prep-manifest.md) |
| 12 | P1 | comparison | TopN — the single most complex numerical query — has **no execution-level parity test** (fixture now supports one) | [sql-numerical-parity](sql-numerical-parity.md) |

P2/cosmetic items are listed in each per-surface doc.

## Per-surface reports

| Surface | Report | Parity verdict |
|---|---|---|
| Critical blockers | [CRITICAL.md](CRITICAL.md) | 2 P0s — fix before cutover |
| Home + nav chrome | [home.md](home.md) | Faithful content; chrome cosmetics diverge |
| Select Datasets | [select-datasets.md](select-datasets.md) | Exists but has the P0 + several P1 correctness gaps |
| Binding | [binding.md](binding.md) | Core SQL faithful (incl. NaN-drop); scatter-filter + symbol + default-regulator gaps |
| Perturbation | [perturbation.md](perturbation.md) | Mirrors Binding 1:1, same gaps (NaN→500 verified already handled) |
| Comparison | [comparison.md](comparison.md) | Strong parity; one numeric dedup divergence + cosmetics |
| SQL / numerical parity | [sql-numerical-parity.md](sql-numerical-parity.md) | Corr faithful; hackett-view filter + TopN test gap |
| App chrome / cross-cutting | [app-chrome.md](app-chrome.md) | Mostly faithful; nav state-loss P1 |
| Data prep / manifests | [data-prep-manifest.md](data-prep-manifest.md) | v4 plumbing complete; content/derivation gaps |
| Verified non-gaps | [non-gaps.md](non-gaps.md) | Things confirmed FINE — don't re-investigate |

## Cross-cutting root cause: the test fixture

Most of the P0/P1s were invisible to CI because the fixture was **one dataset per data_type**, so binding/perturbation/comparison correlations collapsed to empty pair-sets and never produced a multi-sample group, a >1000-regulator intersection, or a real off-diagonal matrix cell. **`9cbc3ae` already widened the binding side** — `harbison` is now a second active binding dataset with an IEEE-NaN cell, a zero-variance column, and a reserved-keyword column, which is what let it pin the NaN class and reserved-keyword paths.

**Remaining fixture work, in priority order:** add a **second perturbation dataset** (un-masks perturbation multi-pair + the corr boxplot across pairs), ensure a **hackett non-analysis-set row** survives into a query (surfaces SQL-1), and add a backend test that applies a **`regulator_locus_tag` filter** to `/selection/matrix` (surfaces the P0). With 2 binding datasets now present, a callingcards×harbison off-diagonal matrix cell + common-regulators flow is testable today.
