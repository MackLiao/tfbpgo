# Self-contained DEMO stack: Traefik (Let's Encrypt + nip.io) + the Go service.
# Brought up by infra/ cloud-init on a throwaway EC2 instance. Unlike the
# production docker-compose.yml (which attaches to an EXISTING external Traefik
# and also runs the legacy Shiny app), this file ships its own Traefik and runs
# ONLY tfbp — the point is to load-test the new implementation in isolation.
#
# All values come from the .env that cloud-init renders. No secrets: the S3
# artifact download uses the EC2 instance profile via IMDS.

services:
  traefik:
    image: traefik:3.3
    restart: unless-stopped
    command:
      - --providers.docker=true
      - --providers.docker.exposedbydefault=false
      - --providers.docker.network=web
      - --accesslog=true
      - --entrypoints.web.address=:80
      - --entrypoints.web.http.redirections.entrypoint.to=websecure
      - --entrypoints.web.http.redirections.entrypoint.scheme=https
      - --entrypoints.websecure.address=:443
      - --certificatesresolvers.le.acme.email=${ACME_EMAIL}
      - --certificatesresolvers.le.acme.storage=/letsencrypt/acme.json
      # ACME directory. Defaults to LE production; set ACME_CASERVER to the
      # staging URL for a dry-run that doesn't spend nip.io's rate-limit slots.
      - --certificatesresolvers.le.acme.caserver=${ACME_CASERVER:-https://acme-v02.api.letsencrypt.org/directory}
      - --certificatesresolvers.le.acme.httpchallenge=true
      - --certificatesresolvers.le.acme.httpchallenge.entrypoint=web
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - letsencrypt:/letsencrypt
    networks: [web]
    security_opt:
      - no-new-privileges:true

  tfbp:
    # Image repo is configurable so a fork can pull its own GHCR package
    # (the default org may not be writable from your CI). TFBP_IMAGE + TAG
    # are written into .env by cloud-init from the image_repo / image_tag vars.
    image: ${TFBP_IMAGE:-ghcr.io/mackliao/tfbpgo}:${TAG:-latest}
    restart: unless-stopped
    environment:
      DUCKDB_PATH: /data/tfbp.duckdb
      DUCKDB_TEMP_DIR: /tmp/duckdb
      CACHE_SIZE_BYTES: "134217728"
      LOG_LEVEL: info
      PORT: "8080"
      # Sized for the t3.large (8GB) demo box. DUCKDB_MEMORY_LIMIT is DuckDB's
      # buffer-pool cap; 2GB lets it hold more hot blocks and spill less. (Was
      # 800MB for the original t3.small.)
      DUCKDB_MEMORY_LIMIT: ${DUCKDB_MEMORY_LIMIT:-2GB}
      DUCKDB_MAX_TEMP_SIZE: ${DUCKDB_MAX_TEMP_SIZE:-2GB}
      DB_MAX_OPEN_CONNS: ${DB_MAX_OPEN_CONNS:-2}
      # DuckDB per-query thread cap. 2 (vs the §6.3 default of 1) uses BOTH
      # vCPUs for a single heavy query — e.g. the all-pairs perturbation
      # correlation (C(6,2)=15 self-join + corr() branches) that otherwise
      # pegs one core and 504s past the 30s budget. Safe here: the t3.large has
      # 2 vCPUs + RAM headroom, and tests/parity passes at DUCKDB_THREADS=2.
      DUCKDB_THREADS: ${DUCKDB_THREADS:-2}
      MAX_INFLIGHT_REQUESTS: ${MAX_INFLIGHT_REQUESTS:-128}
      # Cap binding×perturbation pairs per comparison. Lower than the binary
      # default (12) because the burstable t3.small runs the single-threaded
      # B×P UNION ~3-5x slower; 6 pairs keeps a cold comparison inside the 30s
      # budget. Pair with the comparison semaphore so even a slow one can't
      # block the site. Raise on a bigger instance.
      MAX_COMPARISON_PAIRS: ${MAX_COMPARISON_PAIRS:-6}
    volumes:
      - tfbp_data:/data:ro
      - tfbp_tmp:/tmp/duckdb
    networks: [web]
    # mem_limit MUST exceed the artifact size + working set — not merely "enough
    # heap". In cgroup v2 the artifact's file-backed page cache is charged to
    # THIS container, so a limit below the artifact size makes the kernel evict
    # and re-read it from EBS on every broad scan (page-cache thrash). With this
    # at 1.6g on the old t3.small the box froze: ~8GB/5min of EBS reads on a
    # 2.9GB file, I/O queue backed up, even /healthz + SSM hung while CPU sat at
    # ~50%. The schema-v6 artifact grew to ~7.9GiB (11 promoter-set variants), so
    # 6g is no longer enough: 12g on a 16GB t3.xlarge holds the whole artifact +
    # DuckDB buffer + Go heap, leaving ~4GB for the OS + Traefik. Raising the box
    # RAM alone does NOT fix it unless this limit is raised too.
    # memswap_limit == mem_limit ⇒ swap off (the box has none anyway).
    mem_limit: ${TFBP_MEM_LIMIT:-12g}
    memswap_limit: ${TFBP_MEM_LIMIT:-12g}
    cap_drop: [ALL]
    security_opt:
      - no-new-privileges:true
    pids_limit: 512
    # Liveness only (/healthz, no DB). Overrides the image's HEALTHCHECK so a
    # busy 2-conn pool under load can't flap the container "unhealthy" and make
    # Traefik's docker provider drop the single backend (caused ~6% 404s in the
    # k6 run). /readyz is for startup gating + multi-replica routing, not
    # container liveness.
    healthcheck:
      test: ["CMD", "wget", "-qO-", "--tries=1", "--timeout=2", "http://127.0.0.1:8080/healthz"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s
    labels:
      - traefik.enable=true
      - traefik.docker.network=web
      - traefik.http.routers.tfbp.rule=Host(`${DEMO_HOST}`)
      - traefik.http.routers.tfbp.entrypoints=websecure
      - traefik.http.routers.tfbp.tls.certresolver=le
      - traefik.http.services.tfbp.loadbalancer.server.port=8080
      # NO active healthcheck here. With a SINGLE replica, an active /readyz
      # probe that false-positives under load (the 2-conn DuckDB pool starves
      # the canary SELECT past the timeout) makes Traefik de-route the only
      # backend → 503s a burst of live traffic with no failover benefit. The
      # k6 warm run proved this: the app returned 200 to 98.9% of requests it
      # received, but Traefik 503'd ~800 it de-routed during probe flaps.
      # Re-add a healthcheck only with >1 replica AND a /readyz that uses a
      # dedicated DB connection so it can't starve.

  tfbp-data-init:
    image: amazon/aws-cli:2.35.2
    restart: "no"
    profiles: ["init"]
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        set -euo pipefail
        # Fail CLOSED on a missing/malformed checksum — see the prod
        # docker-compose.yml init container for the rationale (the init step is
        # the sole §9.5 artifact-integrity gate; a vacuous sha256sum -c on an
        # empty SHA must never silently "pass").
        expected_sha="$${ARTIFACT_SHA256:-}"
        case "$$expected_sha" in
          "" | *[!0-9a-fA-F]*) echo "FATAL: ARTIFACT_SHA256 must be set to 64 hex chars" >&2; exit 1 ;;
        esac
        [ "$${#expected_sha}" -eq 64 ] || { echo "FATAL: ARTIFACT_SHA256 must be 64 hex chars (got $${#expected_sha})" >&2; exit 1; }
        trap 'rm -f /data/tfbp.duckdb.new' EXIT
        ok=""
        for i in 1 2 3; do
          if aws s3 cp "s3://$${ARTIFACT_BUCKET}/$${ARTIFACT_KEY}" /data/tfbp.duckdb.new; then
            ok=1; break
          fi
          echo "download attempt $$i failed; retrying in $$((i*10))s" >&2
          sleep $$((i*10))
        done
        [ -n "$$ok" ] || { echo "FATAL: download failed after 3 attempts" >&2; exit 1; }
        echo "$$expected_sha  /data/tfbp.duckdb.new" | sha256sum -c --status - \
          || { echo "FATAL: SHA256 mismatch on downloaded artifact" >&2; exit 1; }
        mv /data/tfbp.duckdb.new /data/tfbp.duckdb
        echo "artifact installed: $$(wc -c < /data/tfbp.duckdb) bytes"
    environment:
      # Credentials come from the EC2 instance profile via IMDS — no keys here.
      AWS_REGION: ${AWS_REGION:-us-east-2}
      ARTIFACT_BUCKET: ${ARTIFACT_BUCKET}
      ARTIFACT_KEY: ${ARTIFACT_KEY}
      ARTIFACT_SHA256: ${ARTIFACT_SHA256}
    volumes:
      - tfbp_data:/data

volumes:
  tfbp_data:
  tfbp_tmp:
  letsencrypt:

networks:
  web:
    external: true
